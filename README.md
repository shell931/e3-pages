# Visor (comité)

Sitio: https://shell931.github.io/e3-pages/

Pide usuario y contraseña. Los datos van **cifrados** (AES-GCM) en `data/vault.json`. Pedí las claves al equipo; no están en este README.

Este repo es público a propósito: GitHub Pages en cuenta gratuita no funciona sobre un repo privado. El repo privado `shell931/e3` (visor local + `resultados.json`) **no** se hace público.

El login no es una cuenta de GitHub: es la clave que descifra el vault en el navegador.
